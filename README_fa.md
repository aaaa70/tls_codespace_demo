# 🌐 اجرای سرور و کلاینت رمزنگاری‌شده (TLS) و وب Flask در GitHub Codespaces

این پروژه برای یادگیری مفاهیم **TLS/SSL** و اجرای امن در محیط ابری **GitHub Codespaces** طراحی شده است.

---

## 🧩 فایل‌ها
- `server.py` → سرور TCP امن با TLS
- `client.py` → کلاینت TLS برای ارتباط با سرور
- `webapp.py` → برنامه Flask که روی HTTPS اجرا می‌شود
- `generate_certs.sh` → ساخت گواهی self-signed
- `.devcontainer/devcontainer.json` → تنظیمات محیط Codespace
- `requirements.txt` → نیازمندی‌ها (Flask)

---

## 🚀 اجرای پروژه در Codespaces

1. تمام فایل‌ها را در ریپوی خود قرار دهید و **Commit & Push** کنید.
2. در GitHub روی **Code → Codespaces → Create Codespace on main** کلیک کنید.
3. وقتی محیط آماده شد، ترمینال باز کنید (گواهی‌ها خودکار ساخته می‌شوند).

### اجرای سرور TLS
```bash
python3 server.py
```

### اجرای کلاینت TLS
در یک ترمینال جدید:
```bash
python3 client.py
```

### اجرای وب‌سرور Flask
```bash
python3 webapp.py
```

سپس در Codespaces روی دکمه “Ports” کلیک کنید → پورت 5000 را باز کنید → گزینه “Open in Browser” را بزنید.

✅ صفحه‌ی امن HTTPS در مرورگر باز می‌شود.

---

## ⚙️ نکات
- این پروژه صرفاً برای آموزش است (نه برای استفاده در محیط واقعی).
- گواهی self-signed فقط برای تست مناسب است.
- می‌توانید با `openssl` گواهی جدید ایجاد کنید.

---
**تهیه‌شده برای یادگیری ایمن شبکه و رمزنگاری.**
